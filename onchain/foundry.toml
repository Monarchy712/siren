[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.24"
optimizer = true
optimizer_runs = 200

# Base Sepolia. RPC is passed explicitly on the CLI (https://sepolia.base.org);
# this alias is a convenience only.
[rpc_endpoints]
base_sepolia = "https://sepolia.base.org"
