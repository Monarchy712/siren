cache/
out/
broadcast/
# local real addresses (never commit)
.env
# disposable counterparty keystores (receive-only; keys never used)
counterparties/
# seeding output
seed_log.tsv
deployment.txt
# never commit any password material
*.kspw
.kspw
